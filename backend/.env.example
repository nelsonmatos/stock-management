# PostgreSQL connection string
# For Railway: Railway provides this automatically when you add a PostgreSQL service
# For Supabase: Settings > Database > Connection String > URI
# For Neon:     Dashboard > Connection Details > Connection string
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# Set to "production" when running in prod (enables static file serving)
NODE_ENV="development"
